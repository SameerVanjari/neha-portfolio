"use client";

import type { Theme } from "@/data/themes";

export default function BottomNav({ theme }: { theme: Theme }) {
  // Nav items moved back to navbar — BottomNav retired (kept for layout compat, renders nothing)
  return null;
}
